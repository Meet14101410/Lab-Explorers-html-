<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Lab Explorers</title>
<style>
  body{margin:0;font-family:system-ui,Arial;background:#0b1020;color:#fff;display:grid;place-items:center;height:100vh}
  #hud{position:fixed;top:12px;left:12px;background:#1b2550aa;padding:8px 12px;border-radius:12px}
  canvas{background:#0f1633;border:3px solid #4ae3ff;border-radius:16px;box-shadow:0 10px 30px #0008}
  .btn{position:fixed;bottom:14px;right:14px;background:#4ae3ff;color:#002733;border:0;border-radius:12px;padding:.6rem .9rem;font-weight:700}
</style>
</head>
<body>
<div id="hud">🔬 Score: <span id="s">0</span> | ⏱️ <span id="t">30</span>s</div>
<canvas id="game" width="480" height="320"></canvas>
<button class="btn" onclick="restart()">Restart</button>

<script>
const c = document.getElementById("game"), x = c.getContext("2d");
let keys={}, score=0, time=30, over=false;
const player = {x:40,y:150,r:12,spd:2.6};
const flasks = [...Array(5)].map(spawnFlask);

function spawnFlask(){ // beaker/atom pickup
  return {x: 80+Math.random()*370, y: 20+Math.random()*280, r:10+Math.random()*4, dx:(Math.random()*.6+.2)*(Math.random()<.5?-1:1)};
}

addEventListener("keydown",e=>keys[e.key]=true);
addEventListener("keyup",e=>keys[e.key]=false);

function circle(a){x.beginPath();x.arc(a.x,a.y,a.r,0,Math.PI*2);x.fill()}

function drawPlayer(){
  // little scientist: head+body
  x.fillStyle="#ffdca8"; circle(player);                       // head
  x.fillStyle="#6ee7ff"; x.fillRect(player
